class Place {
  constructor(id, title, imageUri, address, lat, log) {
    this.id = id;
    this.title = title;
    this.imageUri = imageUri;
    this.address = address;
    this.lat = lat;
    this.log = log;
  }
}

export default Place;
